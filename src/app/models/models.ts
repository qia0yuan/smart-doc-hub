export interface User {
    // name: string;
    // email: string;
    // phone: string;
    token: string;
    accountId: number;
}

export interface ToastConfig {
    type?: string;
    title?: string;
    message?: string;
}